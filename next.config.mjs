/** @type {import('next').NextConfig} */
// import path from 'node:path';


const nextConfig = {
    // webpack: (config) => {
    //     // Añade el alias @ para apuntar a la carpeta src
    //     config.resolve.alias['@'] = path.resolve(__dirname, 'src');
    //     return config;
    //   },
};


export default nextConfig;
